# CLAUDE.md

이 문서는 이 저장소(McOlympics 레크레이션 점수판 React 포팅)에서 AI 에이전트가 작업할 때 반드시 지켜야 할 규칙을 정의합니다.

## 프로젝트 개요

- 목표: `design-reference/레크레이션 점수판.dc.html`에 이미 검증된 프로토타입을 **React 19 + TypeScript + Tailwind CSS v4** SPA로 픽셀·로직 단위로 동일하게 포팅.
- 상세 구현 계획: `docs/superpowers/plans/2026-08-10-recreation-scoreboard-react-port.md` (12개 Task, 각 Task는 완성된 코드와 단계별 체크리스트 포함). 모든 작업은 이 문서를 근거로 진행하고, 임의로 로직/카피/수치를 바꾸지 않는다.
- 원본 로직/카피의 유일한 근거는 `design-reference/레크레이션 점수판.dc.html`이다. PIN, 아이템 가격, 룰렛 확률, 대진표 등 모든 리터럴은 이 파일과 반드시 일치해야 한다.

## 코드 설계 원칙

### SRP (단일 책임 원칙)

- **`src/lib/`**: React나 상태에 대한 지식이 전혀 없는 순수 함수만 둔다 (계산, 랭킹, 코드 생성 등). 부수효과 없음, 단위 테스트 필수.
- **`src/types/`**: `state.ts`(앱이 기억하는 상태)와 `view.ts`(페이지가 실제로 받는 props)를 분리한다. 하나의 파일이 두 책임을 동시에 갖지 않는다.
- **`src/hooks/useScoreboard.ts`**: 상태와 액션을 소유하는 유일한 곳. 이 훅 밖에서는 상태를 직접 변경하지 않는다.
- **`src/components/ui/`**: `ScoreboardView`(또는 그 일부)를 모르는, 어디서나 재사용 가능한 범용 컴포넌트만 둔다. 도메인 지식이 하나라도 들어가면 `pages/`로 옮긴다.
- **`src/pages/`**: `Screen` 값 하나당 파일 하나. 로직 없이 `view`를 받아 그리는 선언적 렌더링만 담당한다.
- 새 기능/컴포넌트를 추가할 때 "이 파일이 하나의 이유로만 변경되는가?"를 항상 자문한다. 두 가지 이상의 이유로 변경될 수 있다면 분리한다.

### 유지보수성

- 매직 넘버/하드코딩된 한글 카피는 `design-reference` 원본에서 그대로 가져오되, 반복되는 값은 상수(`scoring.ts`, Tailwind `@theme` 토큰 등)로 한 곳에서만 정의한다.
- 브랜드/시스템 색상은 Tailwind `@theme` 토큰으로만 참조한다 (raw hex 금지). 팀별 동적 색상(`Team.color`)만 예외적으로 inline `style`을 허용한다.
- 모든 px 값은 Tailwind 임의값(`px-[18px]` 등)으로 원본과 동일하게 유지한다.
- 파일 하나가 너무 커지면(대략 200줄 이상, 여러 책임이 섞이면) 즉시 하위 컴포넌트/모듈로 분리한다.
- 새 코드에는 "무엇을 하는지"를 설명하는 뻔한 주석을 달지 않는다. 코드만 봐서는 알 수 없는 의도/트레이드오프가 있을 때만 주석을 남긴다.

## 작업 진행 프로세스 (모든 Task 공통)

각 Task(또는 Task 내 의미 있는 단위)는 아래 순서를 **반드시** 따른다. 중간 단계를 건너뛰지 않는다.

1. **작업할 내용 안내**: 지금부터 진행할 Task 번호/범위, 변경될 파일 목록, 핵심 구현 포인트를 먼저 요약해서 보고한다.
2. **작업 승인**: 사용자의 승인을 받을 때까지 실제 코드 변경을 시작하지 않는다.
3. **작업 진행**: 승인된 범위 안에서만 구현한다. 계획에 없는 범위까지 확장하지 않는다(범위 확장이 필요하면 별도로 안내하고 재승인받는다).
4. **작업 컨펌 및 수정**: 구현 결과(변경 파일, 테스트/빌드 결과, 스크린샷 등 필요한 근거)를 보고하고, 사용자 피드백이 있으면 반영해 수정한다.
5. **커밋 메시지 추천**: 컨펌이 끝나면 Conventional Commits 스타일(`feat:`, `fix:`, `test:`, `refactor:` 등)의 커밋 메시지를 제안한다. 임의로 커밋하지 않는다.
6. **작업자 커밋 진행**: 사용자가 커밋 메시지를 승인하면 그때 커밋을 진행한다.
7. **다음 작업 진행**: 커밋이 끝난 뒤에만 다음 Task로 넘어간다. 여러 Task를 한 번에 묶어 진행하지 않는다.

## 참고

- Task 목록 및 전체 스펙: `docs/superpowers/plans/2026-08-10-recreation-scoreboard-react-port.md`
- 원본 프로토타입: `design-reference/레크레이션 점수판.dc.html`, `design-reference/support.js`
